import React from 'react';

const Header = () => (
  <header className="app-header">
    <h1>Ephemeral Chat</h1>
    <p>Messages are public and disappear after 24 hours. A new User ID is assigned for every message.</p>
  </header>
);

export default Header;
